[build]
  command = "npm run build"
  publish = "dist"
  environment = { NODE_VERSION = "18" }

[build.environment]
  VITE_APP_VERSION = "2.5.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
